object VistaConsola {

    /*
    fun pedirParticipantes() {
        println("Introduce el número de participantes:")
        val numeroParticipantes = readlnOrNull()?.toIntOrNull() ?: 0

        val nombresVehiculos = mutableSetOf<String>()
        val vehiculosParticipantes = mutableListOf<Vehiculo>()

        for (i in 1..numeroParticipantes) {
            var nombreVehiculo: String
            do {
                println("Nombre del vehículo $i:")
                nombreVehiculo = readlnOrNull()?.trim()?.lowercase() ?: ""
            } while (nombreVehiculo.isEmpty() || !nombresVehiculos.add(nombreVehiculo))

            // Generar y asignar un vehículo aleatoriamente aquí
            val vehiculoAsignado = generarVehiculoAleatorio(nombreVehiculo.capitalizar())
            vehiculosParticipantes.add(vehiculoAsignado)

            // Mostrar información del vehículo asignado
            println("Te ha tocado un $vehiculoAsignado")
        }
    }

    // Función auxiliar para generar vehículos aleatoriamente
    fun generarVehiculoAleatorio(nombre: String): Vehiculo {
        // Implementación dependiendo de las reglas específicas para cada tipo de vehículo
        
    }
    */

}